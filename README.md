# Lazymod

Aliases for launching terminal UIs in the lazy tool family.
Provides short commands for docker, git, and ssh dashboards.

## Functions
- None.

## Aliases
- `ld`: Run `lazydocker`.
- `lg`: Run `lazygit`.
- `lsh`: Run `lazyssh`.
